# 「たかがAI、されどAI。」仕様書

## 1. 作品概要

### コンセプト

人間とAIが同じ大喜利のお題に回答し、ほかのプレイヤーが面白い方へ投票する対戦ゲーム。

AIに負けても、連打するなどの**人間の肉体を使った挽回チャレンジ**に成功すれば、人間側の勝利にひっくり返せる。

### 対応環境

- スマートフォン向けWebアプリ
- 縦画面前提
- PWA対応（時間に余裕があれば）
- 1人1台のスマートフォンを使用
- 対応人数：4人固定
- ログイン不要

---

## 2. 勝敗ルール

### 基本構造

プレイヤー全員で構成する**人類チーム**と、AIが対戦する。

```text
人類 0 ─ 0 AI
```

先に3ポイント獲得した側が勝利。

### 1ラウンドの流れ

1. 挑戦者となるプレイヤーを1人決定
2. 大喜利のお題を表示
3. 挑戦者とAIがそれぞれ回答
4. 挑戦者以外のプレイヤーが回答A・Bに投票
5. 人間とAIの回答を公開
6. 人間が勝った場合、人類に1ポイント
7. AIが勝った場合、挽回チャレンジを開始
8. 挽回成功なら人類に1ポイント
9. 挽回失敗ならAIに1ポイント
10. 次のラウンドへ進む

### 挑戦者の決め方

- ゲーム開始時にプレイヤーの順番をランダムで決める
- 同じプレイヤーが連続で挑戦者にはならない
- 全員が1回挑戦するまでは、挑戦済みプレイヤーを再選しない
- 全員が挑戦済みになったら順番を再シャッフルする

### 投票できる人

- 挑戦者本人は投票できない
- それ以外のオンライン状態のプレイヤーが投票する
- 回答が人間とAIのどちらなのかは投票終了まで隠す

---

## 3. 時間設定

| フェーズ | 制限時間 |
|---|---:|
| 大喜利回答 | 45秒 |
| 投票 | 20秒 |
| 挽回チャレンジ | 種類ごとに5～10秒 |
| ラウンド結果 | 5秒 |

ラウンド開始、回答公開、挽回チャレンジ開始前の自動カウントダウンは設けない。

すべての期限はサーバー側で管理し、クライアントには`deadlineAt`を送信する。

クライアント側だけで45秒を数えるのではなく、現在時刻と`deadlineAt`の差から残り時間を表示する。

---

## 4. 大喜利のお題

### MVPで扱うお題

最初はテキスト形式のみとする。

#### お題例

- 絶対に行きたくないコンビニ。その特徴とは？
- 未来の学校では当たり前になっていること
- このAI、ポンコツだな。なぜ？
- 世界一弱そうな必殺技の名前
- 新機能が一つだけ残念なスマートフォン

### お題データ

お題はリポジトリ内のJSONファイルで管理する。

```ts
type Prompt = {
  id: string;
  type: "text";
  text: string;
  category: "normal" | "name" | "fill";
};
```

```json
{
  "id": "prompt_001",
  "type": "text",
  "text": "絶対に行きたくないコンビニ。その特徴とは？",
  "category": "normal"
}
```

### お題の選択ルール

- ゲーム中に同じお題は出さない
- 使用済みのお題IDをルーム状態に保存する
- お題はランダムで選択する
- 最低30問、可能なら50問以上用意する

---

## 5. 人間の回答

### 入力仕様

- 1行回答
- 最大60文字
- 改行不可
- 空白だけの回答は禁止
- 回答時間内であれば修正可能
- 「回答を確定する」を押すとロック
- 確定後は変更不可

### 時間切れ

未確定でも文字が入力されていれば、その内容を自動提出する。

何も入力されていない場合は人間側の敗北扱いとし、投票を省略して挽回チャレンジへ進む。

---

## 6. AIの回答生成

### 使用API

- OpenAI Responses API
- 初期モデル：環境変数で指定
- Cloudflare WorkerからOpenAI APIへ直接接続
- APIキーはCloudflare WorkerのSecretに保存

```env
OPENAI_API_KEY=...
OPENAI_MODEL=...
```

### 生成タイミング

回答フェーズが始まった時点で、AIの回答生成を開始する。

```text
お題表示
├─ 人間が回答を考える
└─ 同時にAI回答を生成
```

AIには人間の回答を送らない。

### AIへの指示

```text
あなたは日本語の大喜利に回答するAIです。

次のお題に対して、最も面白いと思う回答を1つだけ作成してください。

条件:
- 日本語で回答する
- 1行で回答する
- 60文字以内
- 解説を付けない
- 「回答：」などの接頭辞を付けない
- Markdownを使わない
- 実在人物への中傷や差別的な表現を避ける
```

### レスポンス形式

Responses APIの`output_text`から回答文字列を取得し、`validateAiAnswer`で次を検証する。Structured Outputsは使用しない。

- 文字列である
- 1文字以上
- 60文字以内
- 改行を含まない

### AI生成エラー

1. 8秒でタイムアウト
2. 再試行は行わない
3. タイムアウトまたは生成失敗時は、リポジトリに用意した固定回答へ切り替える
4. 固定回答をAI回答として使用し、ラウンドを継続する

---

## 7. 回答の表示と投票

### 匿名化

人間とAIの回答をランダムに回答A・回答Bへ割り当てる。

```ts
type AnswerOrder = {
  A: "human" | "ai";
  B: "human" | "ai";
};
```

割り当てはサーバーで1回だけ決め、全端末で同じ順番を表示する。

### 投票画面

```text
どっちが面白い？

┌─────────────────┐
│ 回答 A           │
│ 24時間ずっと閉店 │
└─────────────────┘

┌─────────────────┐
│ 回答 B           │
│ 店員が全員ラスボス│
└─────────────────┘
```

- 回答カード全体を押して投票
- 投票後の変更は不可
- 誰がどちらに投票したかは公開しない
- 投票数のみ結果画面で表示する

### 回答公開

投票終了後、以下を結果画面へ一度に表示する。

- 回答A・Bの正体
- 投票数
- 勝者
- 人類またはAIの得点変動

---

## 8. 挽回チャレンジ

### 基本ルール

通常投票でAIが勝った場合のみ発生する。

チャレンジを行うのは、そのラウンドの挑戦者。

```text
AIの勝利！
　　↓
しかし……
　　↓
人類には肉体がある
　　↓
挽回チャレンジ！
```

### チャレンジ選択

MVPでは必ず超連打チャレンジを出す。

時間に余裕があり追加チャレンジを実装した場合のみ、挑戦者の端末が対応しているチャレンジの中からサーバーがランダムに選ぶ。

```ts
type ChallengeType =
  | "rapid_tap"
  | "smile"
  | "color"
  | "shout";
```

端末が追加チャレンジに対応していない場合は、超連打チャレンジを出す。

### 判定方式

センサーやカメラの処理は挑戦者の端末内で行う。

クライアントからサーバーへ次を送る。

- 現在の進捗
- 成功または失敗
- チャレンジ終了時刻

ハッカソン版では不正対策は行わず、クライアントの判定結果を信用する。

### 8.1 超連打チャレンジ

#### ルール

5秒以内に35回ボタンを押す。

```text
目標：35回
現在：28回
残り：1.4秒
```

#### 実装

- `click`ではなく`pointerdown`でカウント
- マルチタッチによる同時加算は禁止
- 押すたびの軽い画面演出（時間に余裕があれば）
- 35回到達した時点で即成功

#### MVP

必須実装。

### 8.2 笑顔チャレンジ

#### ルール

8秒以内に、1秒間笑顔を維持する。

#### 使用技術

- `getUserMedia`
- MediaPipe Face Landmarker
- Face Blendshapes

#### 暫定判定

```ts
const smileScore =
  (mouthSmileLeft + mouthSmileRight) / 2;
```

成功条件：

```text
smileScoreが0.60以上
かつ
1000ms以上継続
```

カメラ映像は端末内だけで処理し、サーバーへ画像を送らない。

#### MVP

時間に余裕がある場合に実装。

### 8.3 指定色チャレンジ

#### ルール

10秒以内に、指定された色の物をカメラへ映す。

対象色：

- 赤
- 青
- 緑
- 黄

#### 判定方法

1. カメラ映像をCanvasへ描画
2. 画面中央40%の領域を取得
3. RGBをHSVへ変換
4. 指定色の範囲に入るピクセルの割合を計算
5. 55%以上が指定色の状態を800ms維持したら成功

#### MVP

時間に余裕がある場合に実装。

### 8.4 叫びチャレンジ

#### ルール

6秒以内に叫びゲージを100%にする。

#### 表示上の注意

「100dB以上」などの物理的なデシベル値は使用しない。

表示は次のようにする。

```text
叫びゲージ
████████░░ 82%
```

#### 判定方法

1. 最初の1秒間で環境音を測定
2. Web Audio APIでRMSを算出
3. 環境音の4倍以上の入力を検知
4. 500ms維持したら成功

#### MVP

時間に余裕がある場合に実装。

---

## 9. 機能の優先順位

### 必須機能

- ルーム作成
- ルーム参加
- 4人のプレイヤー同期
- テキストお題
- 挑戦者の回答
- AI回答
- 匿名投票
- 結果公開
- 3ポイント先取
- 超連打チャレンジ
- WebSocket再接続
- スマートフォン向けUI

### 余裕があれば実装

- 笑顔チャレンジ
- 指定色チャレンジ
- 叫びチャレンジ
- PWAインストール
- 効果音
- バイブレーション
- QRコードによる参加
- カメラ・マイクの端末対応状況表示
- ラウンド開始カウントダウン
- 挽回チャレンジ開始前のカウントダウン
- 最終結果の各ラウンド履歴
- 最も票を獲得した人間の回答表示

### 最後に追加

- 戦績保存
- ランキング
- ユーザー登録

---

## 10. 画面一覧

### 10.1 トップ画面

表示内容：

- タイトル
- ゲーム説明
- 「ルームを作る」
- 「ルームに参加」
- 遊び方

```text
たかがAI、
されどAI。

[ ルームを作る ]
[ ルームに参加 ]
```

### 10.2 ルーム作成画面

入力：

- プレイヤー名
- 最大10文字

作成後、ルームコードを表示する。

### 10.3 ルーム参加画面

入力：

- 6桁のルームコード
- プレイヤー名

### 10.4 ロビー画面

表示内容：

- ルームコード
- 参加者一覧
- ホスト表示
- オンライン状態
- 招待用QRコード（時間に余裕があれば）
- ゲーム開始ボタン

ゲーム開始条件：

- 4人参加
- 全プレイヤーが「準備OK」
- ホストのみ開始可能

### 10.5 回答画面

#### 挑戦者

- お題
- 回答入力欄
- 文字数
- 残り時間
- 回答確定ボタン

#### ほかのプレイヤー

```text
タナカが人類の意地を見せています……

AIも回答生成中
```

人間とAIの回答内容はまだ表示しない。

### 10.6 投票画面

- お題
- 回答A
- 回答B
- 残り時間
- 投票済み表示

挑戦者には以下を表示する。

```text
みんなが審査中です
祈って待て
```

### 10.7 回答公開画面

- 人間の回答
- AIの回答
- 各投票数
- 勝者
- 得点

### 10.8 挽回チャレンジ画面

挑戦者：

- チャレンジ説明
- 連打ボタン
- リアルタイム進捗

ほかのプレイヤー：

- 挑戦者名
- 現在の進捗
- 応援表示

### 10.9 最終結果画面

#### 人類の勝利

```text
人類 3 ─ 2 AI

たかがAI、
されどAI。
```

#### AIの勝利

```text
人類 1 ─ 3 AI

人類、アップデート失敗。
```

表示するもの：

- 最終得点
- もう一度遊ぶ
- トップへ戻る

時間に余裕があれば表示するもの：

- 各ラウンド結果
- 最も票を獲得した人間の回答

---

## 11. ゲーム状態

### 状態一覧

```ts
type GamePhase =
  | "LOBBY"
  | "ANSWERING"
  | "VOTING"
  | "REVEAL"
  | "REVENGE_ACTIVE"
  | "ROUND_RESULT"
  | "GAME_RESULT";
```

### 状態遷移

```text
LOBBY
  ↓
ANSWERING
  ↓
VOTING
  ↓
REVEAL
  ├─ 人間勝利 → ROUND_RESULT
  └─ AI勝利   → REVENGE_ACTIVE
                    ↓
               ROUND_RESULT
                    ↓
          ┌─────────┴─────────┐
          ↓                   ↓
      ANSWERING          GAME_RESULT
```

クライアントから「次へ進む」という命令を受けて状態を変えるのではなく、基本的にDurable Objectが状態遷移を管理する。

---

## 12. ルームデータ

```ts
type Player = {
  id: string;
  name: string;
  joinOrder: number;
  isHost: boolean;
  isOnline: boolean;
  isReady: boolean;
};

type Score = {
  human: number;
  ai: number;
};

type RoundState = {
  roundNumber: number;
  challengerId: string;
  prompt: Prompt;
  humanAnswer: string | null;
  aiAnswer: string | null;
  answerOrder: AnswerOrder | null;
  votes: Record<string, "A" | "B">;
  winner: "human" | "ai" | null;
  challengeType: ChallengeType | null;
  challengeResult: "success" | "failure" | null;
};

type RoomState = {
  version: number;
  roomCode: string;
  phase: GamePhase;
  players: Player[];
  score: Score;
  round: RoundState | null;
  challengerQueue: string[];
  usedPromptIds: string[];
  deadlineAt: number | null;
  createdAt: number;
  updatedAt: number;
};
```

`version`は状態更新ごとに増加させる。

クライアントは現在より古い`version`の状態を受信した場合、破棄する。

---

## 13. WebSocket通信

### 接続先

```text
GET /api/rooms/:roomCode/ws
```

接続時に以下を送る。

- `playerId`
- `playerToken`

### クライアントから送信

```ts
type ClientMessage =
  | { type: "READY"; ready: boolean }
  | { type: "START_GAME" }
  | { type: "SUBMIT_ANSWER"; answer: string }
  | { type: "CAST_VOTE"; choice: "A" | "B" }
  | { type: "REVENGE_PROGRESS"; value: number }
  | {
      type: "REVENGE_RESULT";
      result: "success" | "failure";
    }
  | { type: "PING" };
```

### サーバーから送信

```ts
type ServerMessage =
  | {
      type: "STATE_SYNC";
      state: PublicRoomState;
    }
  | {
      type: "ERROR";
      code: string;
      message: string;
    }
  | { type: "PONG" };
```

基本はイベントを細かく送るより、状態変更後に`STATE_SYNC`で正しい状態全体を配信する。

Durable ObjectsではWebSocket Hibernation APIを使用する。

---

## 14. HTTP API

### ルーム作成

```text
POST /api/rooms
```

リクエスト：

```json
{
  "playerName": "タナカ"
}
```

レスポンス：

```json
{
  "roomCode": "7KX4MR",
  "playerId": "player_xxx",
  "playerToken": "token_xxx"
}
```

### ルーム参加

```text
POST /api/rooms/:roomCode/join
```

リクエスト：

```json
{
  "playerName": "サトウ"
}
```

### ルーム確認

```text
GET /api/rooms/:roomCode
```

返す情報：

- ルームが存在するか
- 参加可能か
- 現在人数
- ゲーム開始済みか

ゲーム中の操作は原則WebSocketを利用する。

HTTP APIにはHono RPCを使用する。

---

## 15. 永続化

### 使用するもの

- Durable Objects
- Durable Object Storage

### 使用しないもの

- Supabase
- Cloudflare D1
- R2
- 外部DB

### 保存対象

以下のタイミングでRoomStateを保存する。

- プレイヤー参加・退出
- 準備状態変更
- フェーズ変更
- 回答確定
- 投票
- 挽回チャレンジ結果
- 得点変更
- ゲーム終了

センサーの進捗は頻繁に変化するため、Storageへ毎回保存しない。

Durable Objectのメモリ上の状態は、オブジェクトが破棄された場合に失われるため、復元に必要なゲーム状態はStorageにも保存する。

### データ削除

MVPでは期限切れルームの自動削除を実装しない。

時間に余裕があれば、以下を実装する。

- ゲーム終了後2時間で削除
- 最終操作から6時間経過したルームを削除
- 期限切れルームへのアクセス時にStorageを削除

対戦履歴は残さない。

---

## 16. 再接続

### プレイヤー識別

参加時に発行するもの：

```text
playerId
playerToken
```

ブラウザの`localStorage`へ保存する。

### WebSocket切断時

切断から2秒後に再接続する。

再接続後、サーバーから最新の`STATE_SYNC`を取得する。

挑戦者・投票者・ホストの切断に対する個別の例外処理や、ホスト権限の自動移譲はMVPでは実装しない。

---

## 17. 技術構成

```text
ブラウザ
├─ React
├─ Vite
├─ TypeScript
├─ Tailwind CSS
├─ Zustand
├─ Zod
├─ vite-plugin-pwa（時間に余裕があれば）
└─ MediaPipe Tasks Vision（時間に余裕があれば）

Cloudflare
├─ Workers
├─ Static Assets
├─ Hono
├─ Durable Objects
├─ Durable Object Storage
├─ WebSocket Hibernation
└─ Wrangler

AI
└─ OpenAI Responses API
```

---

## 18. フォルダ構成

```text
src/
├─ client/
│  ├─ components/
│  ├─ pages/
│  ├─ features/
│  │  ├─ lobby/
│  │  ├─ game/
│  │  ├─ voting/
│  │  └─ challenges/
│  ├─ hooks/
│  ├─ stores/
│  ├─ lib/
│  └─ main.tsx
│
├─ server/
│  ├─ index.ts
│  ├─ routes/
│  ├─ durable-objects/
│  │  └─ GameRoom.ts
│  ├─ services/
│  │  └─ ai.ts
│  └─ lib/
│
├─ shared/
│  ├─ types/
│  ├─ schemas/
│  ├─ messages/
│  └─ constants.ts
│
└─ data/
   └─ prompts.json
```

共有型は`src/shared`へ置き、フロントとサーバーの両方から利用する。

---

## 19. 定数

```ts
export const GAME_CONFIG = {
  minPlayers: 4,
  maxPlayers: 4,
  winningScore: 3,

  answerTimeMs: 45_000,
  voteTimeMs: 20_000,
  roundResultMs: 5_000,

  maxPlayerNameLength: 10,
  maxAnswerLength: 60,

  aiTimeoutMs: 8_000,
  reconnectDelayMs: 2_000,
} as const;
```

チャレンジ設定：

```ts
export const CHALLENGE_CONFIG = {
  rapidTap: {
    durationMs: 5_000,
    requiredCount: 35,
  },
  smile: {
    durationMs: 8_000,
    threshold: 0.6,
    requiredDurationMs: 1_000,
  },
} as const;
```

---

## 20. セキュリティ・プライバシー

- OpenAI APIキーをフロントエンドへ渡さない
- APIキーはWorker Secretに保存
- プレイヤー回答をHTMLとして描画しない
- 回答文字数をサーバー側でも検証
- WebSocketメッセージをZodで検証
- ホスト操作には`playerToken`を要求
- ルームコードだけではプレイヤーを操作できない
- カメラ映像をサーバーへ送信しない
- マイク音声を録音・送信しない
- 顔画像を保存しない
- センサー値はチャレンジ終了後に破棄する

---

## 21. エラー表示

ユーザー向けのエラー文は技術用語を使わない。

```text
ROOM_NOT_FOUND
→ そのルームは見つかりませんでした

ROOM_FULL
→ このルームは満員です

GAME_ALREADY_STARTED
→ このゲームはすでに始まっています

NAME_ALREADY_USED
→ 同じ名前のプレイヤーがいます

AI_GENERATION_FAILED
→ AIが回答中にフリーズしました。代わりの回答で続けます
```

---

## 22. MVP完成条件

以下をすべて満たせば、ハッカソン提出可能な完成状態とする。

1. 4台のスマートフォンが同じルームへ参加できる
2. ホストがゲームを開始できる
3. 挑戦者が順番に選ばれる
4. テキストお題が表示される
5. 人間とAIが回答できる
6. 回答A・Bが匿名で表示される
7. 挑戦者以外が投票できる
8. 投票結果と回答者を公開できる
9. AI勝利時に挽回チャレンジが始まる
10. 超連打チャレンジで成功判定できる
11. 成功・失敗に応じて得点が変わる
12. 3ポイント先取でゲームが終了する
13. 一時的な通信切断から復帰できる
14. カメラやDBがなくてもゲーム全体が成立する

---

## 23. 実装順

### 第1段階：通信だけ完成させる

- ルーム作成
- ルーム参加
- Durable Object
- WebSocket接続
- プレイヤー一覧同期

### 第2段階：AIなしでゲームを完成させる

- お題表示
- 人間回答
- 仮のAI回答
- 投票
- 得点
- 3ポイント先取

AI回答は最初は固定文字列でもよい。

### 第3段階：OpenAI APIを接続する

- Responses API
- タイムアウト
- 失敗時の固定回答への切り替え
- 回答検証

### 第4段階：挽回チャレンジ

- 超連打
- 進捗同期

### 第5段階：演出（時間に余裕があれば）

- カウントダウン
- 連打中・成功時の演出
- 効果音
- 画面振動
- 紙吹雪
- 最終結果の各ラウンド履歴
- 最も票を獲得した人間の回答表示

### 第6段階：追加機能（時間に余裕があれば）

- 笑顔認識
- 指定色チャレンジ
- 叫びチャレンジ
- PWA
- QRコード
- カメラ・マイクの端末対応状況表示
